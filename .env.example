# Database (Supabase Postgres connection string)
DATABASE_URL="postgresql://user:password@host:5432/localboost?schema=public"

# OpenAI
OPENAI_API_KEY="sk-your-openai-key-here"
OPENAI_MODEL="gpt-4o-mini"

# Shared secret for admin-only API endpoints (e.g. /api/admin/jobs/cleanup).
# Generate with: openssl rand -hex 32
ADMIN_API_TOKEN="replace-with-a-long-random-string-at-least-32-chars"

# Supabase (public client)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
